基于原项目 `https://github.com/linguo2625469/qq-farm-cdp-auto` 对当前工作区做了整理。

这个目录保留了：

- 当前项目实际使用的入口与源码：`run.cjs`、`src/`、`wmpf/`、`button.js`、`qq-host.js`
- 当前项目使用到的页面与脚本：`public/`、`scripts/patch-qq-miniapp.cjs`、`scripts/extract-qq-miniapp-images.cjs`
- 当前版本依赖的配置数据：`gameConfig/`、`data/farm-config.json`、`data/gameConfig/`
- 基本项目文件：`package.json`、`package-lock.json`、`.env`、`.env.example`、`README.md`、`LICENSE`

这个目录没有带入：

- 依赖安装产物：`node_modules/`
- 构建产物：`dist/`
- 临时参考仓库与中间目录：`_tmp_*`、`_codex_ref_original/`、`upstream_merge_tmp/`
- 调试输出与一次性文件：`_lands_*.json`、`_farm_model_debug.json`、`_main_ui_debug.json`、`_tmp_effect_inflated.bin`
- 日志与抓包临时目录：`_shop_trace_*`、`data/runtime-logs/`、`gateway.stdout.log`、`gateway.stderr.log`
- 纯导出型资源：`_miniapp_extracted_images/`
- 未发现被当前项目入口引用的辅助脚本：`scripts/shop-trace-capture.cjs`

轻量校验结果：

- `src/config.js` 可加载
- `src/game-config.js` 可读取 `gameConfig/`
- `src/plant-analytics.js` 可读取 `data/gameConfig/`

如果你希望，我下一步可以继续把这个整理版目录直接初始化成一个干净仓库，或者再帮你生成一份“只保留源码、不含 .env”的可分发版本。
